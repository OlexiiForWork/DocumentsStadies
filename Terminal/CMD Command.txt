## Команды CMD !!!!!!!!!!!!!!!!!!!!!!!

Получаем список портов которые прослушываються:
|
----netstat -an

Получаем список процесов и их <PID> которые прослушываються:
|
----netstat -ano | findstr <PORTS>

Удаление/Остановка процеса по <PID> процеса
|
----taskkill /PID <PID> /F